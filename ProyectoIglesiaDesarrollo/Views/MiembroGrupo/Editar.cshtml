@model MiembroGrupoVm
@{
    ViewData["Title"] = "Editar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>
    <form method="post" asp-action="Editar">
        <input asp-for="Id" type="hidden" />
        <div class="form-group">
            <label for="GrupoServicioId" class="control-label">Grupo al que Pertenece</label>
            @Html.DropDownList("GrupoServicioId", Model.GrupoServicios, "Seleccione un Grupo", new { @class = "form-control" })
        </div>
        <div class="form-group">
            <label for="MiembroId" class="control-label">Miembro</label>
            @Html.DropDownList("MiembroId", Model.Miembros, "Seleccione un Miembro", new { @class = "form-control" })
        </div>
        <div class="form-group">
            <input type="submit" value="Guardar" class="btn-success">
        </div>
    </form>
</div>